<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detail News</title>
    <link rel="stylesheet" href="assets/css/bootstrap.css">
    <link rel="stylesheet" href="assets/css/detailnews.css">
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a href="/" class="navbar-brand">News</a>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item active">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/user">Add User</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/news">Add News</a>
                    </li>
                </ul>
            </div>
        </nav>
    </header>
    <main>
        {{#each data}}
        <div id="berita">
            <div class="thumbnail">
                <img src="{{image}}" alt="">
            </div>
            <div class="content">
                <div class="title">
                    <h4>{{title}}</h4>
                </div>
                <div class="created">
                    <strong>{{user_id}}</strong>
                </div>
                <div class="berita">
                    <p> {{deskripsi}}</p>
                </div>
                <div class="tombol">
                    <a href="" class="btn btn-sm btn-info" data-id={{id}}>Detail Berita</a>
                </div>
            </div>
        </div>
        {{/each}}
    </main>
</body>
</html>